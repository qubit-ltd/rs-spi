[package]
name = "service-contract"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
service-model = { path = "../service-model" }
spi = { package = "qubit-spi", path = "../../../..", features = ["inventory"] }
